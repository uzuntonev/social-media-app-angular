import { LikeDislikePipe } from './like-dislike.pipe';

describe('LikeDislikePipe', () => {
  it('create an instance', () => {
    const pipe = new LikeDislikePipe();
    expect(pipe).toBeTruthy();
  });
});
